<th>
    <div class="form-group">
        <label for="id-{{$name}}" class="col-form-label">{{$name}}</label>
        <input id="id-{{$name}}" class="form-control" name="{{$name}}" value="{{ old($name) }}">
    </div>
</th>
